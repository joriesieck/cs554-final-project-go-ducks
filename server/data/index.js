const userData = require('./users');
const friendData = require('./friends');
const leaderboardData = require('./leaderboard');

module.exports = {
  users: userData,
  friends: friendData,
  leaderboard: leaderboardData
};
